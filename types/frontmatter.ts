export type AboutFrontmatter = {
  title: string;
  description: string;
};

export type ProjectsFrontmatter = {
  title: string;
  description: string;
};

export type BlogFrontmatter = {
  title: string;
  description: string;
  date: string;
  // foo, bar, baz
  authors: string;
};
